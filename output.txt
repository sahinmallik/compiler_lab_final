#id <id.id>
#id <id.id>

id id()
{
    id id[100], id[100];
    id id, id, id;

    id("id id id: ");
    id("%id", id);

    id = id(id);
    
    id (id = id - 1, id = 0; id >= 0; id--, id++)
    {
        id[id] = id[id];
    }

    id[id] = '\0';

    id("id id id id id id: %id", id);

    id 0;
}
